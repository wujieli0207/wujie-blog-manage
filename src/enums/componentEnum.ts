// 表单类型
export enum EFormType {
  INPUT = "input",
  TEXTAREA = "textarea",
  SELECT = "select",
  RADIO = "radio",
  CHECKBOX = "checkbox",
  SWITCH = "switch",
  DATE = "date",
  UPLOAD = "upload",
}
